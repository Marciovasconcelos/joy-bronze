import {auth,db} from "./firebase-config.js";
import {onAuthStateChanged,signOut} from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
import {collection,getDocs,doc,getDoc,query,where,updateDoc} from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";
const $=s=>document.querySelector(s);
async function agenda(){const data=$("#filtroData").value;const q=data?query(collection(db,"agendamentos"),where("data","==",data)):collection(db,"agendamentos");const s=await getDocs(q);$("#agenda").innerHTML=s.empty?"Nenhum agendamento.":s.docs.map(x=>{const a=x.data();return `<div class="appointment"><b>${a.data} ${a.horario}</b><br>${a.nome||"Cliente"}<br>📱 ${a.telefone||""}<br>${a.servico||""}<br><select data-status="${x.id}"><option ${a.status==="pendente"?"selected":""}>pendente</option><option ${a.status==="confirmado"?"selected":""}>confirmado</option><option ${a.status==="concluido"?"selected":""}>concluido</option><option ${a.status==="cancelado"?"selected":""}>cancelado</option></select></div>`}).join("");document.querySelectorAll("[data-status]").forEach(el=>el.onchange=()=>updateDoc(doc(db,"agendamentos",el.dataset.status),{status:el.value}))}
$("#filtroData").onchange=agenda;$("#logout").onclick=()=>signOut(auth);
onAuthStateChanged(auth,async u=>{if(!u){location.href="admin-login.html";return}const p=await getDoc(doc(db,"usuarios",u.uid));if(!p.exists()||p.data().tipo!=="admin"){location.href="index.html";return}agenda()});